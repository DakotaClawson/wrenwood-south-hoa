import NavDrawer from '../components/NavDrawer';
import { Typography } from '@material-ui/core';

export default function BoardMeetingMinutes() {
    return (
        <NavDrawer>
            <Typography variant="h3" color="primary">
                Board Meeting Minutes
            </Typography>
        </NavDrawer>
    );
}
