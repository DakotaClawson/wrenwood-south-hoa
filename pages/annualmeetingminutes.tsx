import NavDrawer from '../components/NavDrawer';
import { Typography } from '@material-ui/core';

export default function AnnualMeetingMinutes() {
    return (
        <NavDrawer>
            <Typography variant="h3" color="primary">
                Annual Meeting Minutes
            </Typography>
        </NavDrawer>
    );
}
